abi <abi/4.0>,
#include <tunables/global>

profile local_ha-cops flags=(attach_disconnected,mediate_deleted,complain) {
  # Basic Home Assistant apparmor profile from https://developers.home-assistant.io/docs/apps/presentation/#apparmor

  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,

  # Basic profile ends

  # Networking
  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,
  network unix,
  network unix stream,
  network unix dgram,

  # nginx Capabilities
  capability setgid,
  capability setuid,
  capability dac_override,
  capability chown,
  capability kill,
}