#include <tunables/global>

profile local_ha-cops flags=(attach_disconnected,mediate_deleted,complain) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # 1. FIX THE SOCKET CREATION (The error=-13 fix)
  network inet stream,
  network inet6 stream,
  network unix stream,

  # 2. GRANT CATEGORIES (The "Plumbing")
  capability,
  signal,
  ptrace,
  unix,

  # 3. STOP THE //null TRANSITIONS
  # This is the single rule that forces everything into this profile
  # so your audit logs are actually useful.
  /** ix,

  # 4. CAPTURE THE DATA (The "Objective")
  # We allow everything in complain mode so we can see the paths.
  / r,
  /** rwlkm,

  # 5. KNOWN PATHS
  /data/** rw,
  /cops/library/** rwlkm,
  /media/** rwlkm,
  /etc/rsyncd.conf r,
}